"""Code for "When Generative Models Act as Simulators".

Packages: ``environments`` (Othello and Rayworld), ``models`` (Transformer-L), ``probes``
(reading state from the residual stream), ``editors`` (PI, GS, IM), ``metrics`` (Probe Skill,
Edit Index, Edit Fidelity), ``training``, ``scoring`` (``scores.json``) and ``figures``.
"""
