"""The two environments: ``rayworld`` (moving discs seen through a 1D ray cast; frame regression)
and ``othello`` (synthetic Othello games; next-move classification).

Import a subpackage explicitly (``from pim.environments import rayworld``); every dataset path
comes from ``layout``.
"""
